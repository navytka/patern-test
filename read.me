Installation guide:

1. First of all install gulp-cli, to make it run in console:
npm install --global gulp-cli

2. To install all npm dependency run in console:
npm install

3. run in console:
gulp sass

3.1. To run watch for scss files run 'sass:watch' in console.
